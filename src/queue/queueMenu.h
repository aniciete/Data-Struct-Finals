#ifndef QUEUE_MENU_H
#define QUEUE_MENU_H

/**
 * @file queueMenu.h
 * @brief This file contains the declaration for the queue menu function.
 *
 * The `showQueueMenu` function is responsible for displaying the user interface
 * for queue operations, allowing the user to choose between different queue
 * implementations and perform various actions on them.
 */

/**
 * @brief Displays and handles the main queue menu.
 *
 * This function provides a user interface to select between static and dynamic
 * queue implementations and navigate back to the main menu.
 */
void showQueueMenu();

#endif // QUEUE_MENU_H
